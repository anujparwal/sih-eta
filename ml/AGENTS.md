# Machine learning

Follow the root guide. This directory is a placeholder until Phase 5.
Target XGBoost with SHAP explanations and a current-delay carryover baseline.
Reuse backend feature definitions when implemented; do not duplicate them.
Use a time-based evaluation split and report measured MAE/RMSE in minutes.
Do not run model training during a multi-file implementation task.

Store reviewed model exports in `models/` and evaluation reports in `results/`.
Document training data provenance and artifact metadata. Do not claim accuracy
improvements without evidence. No ML tests or model artifacts exist in Phase 1.
